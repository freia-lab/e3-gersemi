The database files to be used with the local iocsh for testing the loop:
Gersemi command -> Linde -> acknowledge to Gersemi.
Start the ioc with the command:
iocsh ./<db_name>

The two variants of the db differ on condition when the acknowledge is sent back to Gersemi.

